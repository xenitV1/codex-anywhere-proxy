/**
 * codex-anywhere — Entry Point
 *
 * Use Codex CLI with any OpenAI-compatible API provider.
 * No OpenAI account needed. No fork needed.
 *
 * Run: bun run proxy.ts
 */

import { startServer } from "./src/server.js";

startServer();
